import React from "react";
import MainPage from "@components/MainPage";

const mainPage = () => {
    return (
        <div>
            <MainPage />
        </div>
    );
};

export default mainPage;
